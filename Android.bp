cc_binary {
    name: "overlay",
    srcs: ["overlay.cpp"],

    shared_libs: [
        "libgui",
        "libui",
        "libandroid",
        "libutils",
        "liblog",
        "libcutils",
    ],

    // Cho phép build cả bản ARM (target) lẫn bản chạy trên Linux (host)
    host_supported: true,
}
